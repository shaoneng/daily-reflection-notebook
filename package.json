{
  "name": "daily-reflection-notebook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A mobile-first daily Markdown journal with AI-assisted reflection, deployable on Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-05-15 --kv=NOTES_KV",
    "deploy": "wrangler pages deploy public",
    "check": "node --check functions/api/day/index.js && node --check functions/api/review/index.js && node --check functions/api/search/index.js && node --check functions/api/export/index.js && node --check functions/api/tags/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.15.0"
  }
}
